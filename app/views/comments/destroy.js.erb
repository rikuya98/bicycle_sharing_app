let commentsArea = document.getElementById("comments");
commentsArea.innerHTML = "<%= j(render 'comments/index', { comments: @comment.article.comments }) %>";
document.querySelector("textarea").value = "";

<%# let form = document.querySelector('#comment-form');
form.removeAttribute('data-ujs-remote');
Rails.delegate(form, 'ajax:success', form, form.getAttribute('data-ujs-event')); %>
